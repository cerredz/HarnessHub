# Declarative Harness Manifest Tickets

1. Ticket 1: Add the shared typed harness manifest layer and register the existing harness metadata.
2. Ticket 2: Refactor CLI/runtime parameter parsing to consume the manifests, then document and test the new shared boundary.
