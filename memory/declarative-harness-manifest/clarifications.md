No blocking clarifications were required after Phase 1.

Implementation will proceed with:

- a new shared manifest type and registry under `harnessiq/shared/`
- concrete manifests for the existing harnesses
- CLI/runtime normalization refactored to delegate to those manifests while preserving current public helper functions
