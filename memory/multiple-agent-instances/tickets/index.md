1. Ticket 1: Add a shared agent-instance registry and base runtime identity model
   Local artifact: `memory/multiple-agent-instances/tickets/ticket-1.md`
   GitHub issue: blocked in this environment unless networked `gh` access is available.
   Status: implemented locally

2. Ticket 2: Wire concrete harnesses and SDK exports into payload-driven instance resolution
   Local artifact: `memory/multiple-agent-instances/tickets/ticket-2.md`
   GitHub issue: blocked in this environment unless networked `gh` access is available.
   Status: implemented locally

Phase 3a complete

Phase 3b note:
- GitHub issue creation is not being executed here because the environment has restricted network access and the task can be completed locally without remote issue creation.
