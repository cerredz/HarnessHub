No blocking clarifications surfaced after Phase 1.

Execution choices:

- New issues will be created only for untracked, concrete debt items to avoid duplicating already-open GitHub issues and PRs.
- Implementation work will be isolated in worktrees based on `origin/main` so the current dirty local branch is not disturbed.
