1. Ticket 1: Refactor Instagram agent tooling and Playwright helpers for PR #163 review feedback
   Implements the owner review comments by moving Instagram tool definitions into `harnessiq/tools`, extracting reusable Playwright helpers into `harnessiq/providers/playwright`, centralizing shared constants, and updating tests.

Phase 3a complete

Note:
- This task is already scoped to an existing pull request (`#163`) on its own branch (`feature/instagram-keyword-agent`).
- Creating additional GitHub issues/PRs for the same bounded review-feedback task would duplicate the active review workflow, so execution proceeds directly on the existing PR branch/worktree.

Phase 3 complete
