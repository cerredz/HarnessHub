No blocking clarifications were required after Phase 1.

Proceeding assumptions:

- The new email harness is an abstract reusable base class rather than a concrete ready-to-run campaign agent.
- Resend credentials are accepted as runtime configuration and are only exposed to the model in masked/redacted form.
- The Resend tooling surface is implemented as a single MCP-style request tool backed by a complete operation catalog for the current stable API surface.
