No blocking ambiguities remain after Phase 1.

- The request is concrete: add one bundled master prompt asset with key `mission_driven`.
- The only inferred work beyond the prompt file itself is updating the catalog tests that hard-code the expected prompt keys.
- No user clarification is required before implementation.
