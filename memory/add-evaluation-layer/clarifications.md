## Phase 2

No blocking clarifications were required after Phase 1.

Working assumptions carried into implementation:

- “pi test” is treated as `pytest`.
- The requested “very basic evaluations” will be delivered as reusable, pytest-friendly evaluation primitives and a small amount of example suite scaffolding rather than a broad real benchmark corpus.
- The new layer will be categorized by behavior (`correctness`, `tool_use`, `efficiency`, `output`) in line with the article’s recommendation to group evals by what they test.
