Post-implementation critique surfaced one concrete cleanup item: after moving helper logic into local/shared helper modules, several agent files still carried stale imports that no longer served any code path. I removed those residual imports from the refactored agent modules, then reran the full compile, agent/CLI test suite, and docs-sync checks. The helper split now reads cleanly, keeps orchestration in `agent.py`, and avoids unnecessary noise in the refactored files.
