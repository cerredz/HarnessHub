No meaningful ambiguities remained after Phase 1.

Working assumptions carried into implementation:
- The request is to remove `artifacts/live_inventory.json` from the committed/generated repo surface entirely.
- The correct fix includes the generator and generated README contract, not only deleting the file.
- Existing unrelated changes in the root checkout must be preserved and avoided during implementation.
