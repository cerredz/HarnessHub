No blocking ambiguities remain after Phase 1.

Proceeding assumptions:
- The two new harnesses will be registered as first-class built-in manifests.
- Shared contracts and durable memory stores will live under `harnessiq/shared/`.
- No new CLI command family will be added unless implementation reveals that manifest registration alone is insufficient.
- JSON subcall orchestration will follow existing repo patterns rather than introducing a remote-recursive “agent spawns agent” runtime.
