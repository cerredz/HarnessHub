Phase 2 review: no blocking clarifications.

The second-pass candidates are structure-preserving refactors with clear compatibility constraints and narrow blast radii. The only meaningful process ambiguity was which codebase state to target; that is resolved by treating `origin/main` as the implementation baseline and the dirty root checkout as user-owned state that will not be modified during implementation.
