# Harnessiq SDK Ticket Index

1. Ticket 1: Rename the source package to harnessiq and stabilize the SDK import surface
   - Make `harnessiq` the real package namespace and preserve the first-class agent/tool API.
   - Status: implemented locally. GitHub issue/PR creation blocked because `gh` is unavailable in this environment.

2. Ticket 2: Add PyPI-style packaging metadata and installation smoke coverage
   - Add build metadata and validation so the SDK can be packaged and installed in standard Python workflows.
   - Status: implemented locally. GitHub issue/PR creation blocked because `gh` is unavailable in this environment.

3. Ticket 3: Document minimal SDK installation and example usage
   - Add lightweight install guidance and a few examples for tools and first-class agent usage.
   - Status: implemented locally. GitHub issue/PR creation blocked because `gh` is unavailable in this environment.
