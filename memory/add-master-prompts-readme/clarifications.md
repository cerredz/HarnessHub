No blocking ambiguities remained after Phase 1.

Implementation will assume the target directory is `harnessiq/master_prompts/` and will enforce the requested future README updates through a regression test instead of relying on manual convention alone.
