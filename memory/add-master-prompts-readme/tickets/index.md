1. Ticket 1: Add a master prompt package README and enforce catalog coverage

   Description: Create `harnessiq/master_prompts/README.md` with the directory overview plus the bundled prompt title/description catalog, and add a regression test that requires future prompt additions to update the README.
