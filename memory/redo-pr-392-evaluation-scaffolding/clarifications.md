No blocking clarifications remain after Phase 1.

The PR review is specific enough to implement directly:

- use plain pytest test functions with docstrings as eval definitions
- support category/tag-based subset execution
- provide lightweight efficiency and judge helpers instead of a registry/check framework
- keep the code surface minimal and readable

Implementation proceeds on that basis.
